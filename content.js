// content.js - Este archivo se mantiene para inyectar el CSS de resaltado (content.css),
// pero ya no necesita escuchar mensajes para el scroll, ya que esa lógica
// se inyecta directamente desde el panel lateral (sidepanel.js).
